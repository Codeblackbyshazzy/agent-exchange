apiVersion: v1
kind: Service
metadata:
  name: aex-credentials-provider
  namespace: aex
  labels:
    app.kubernetes.io/name: aex-credentials-provider
    app.kubernetes.io/component: credentials-provider
    app.kubernetes.io/part-of: agent-exchange
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aex-credentials-provider
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
