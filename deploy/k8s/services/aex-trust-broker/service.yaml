apiVersion: v1
kind: Service
metadata:
  name: aex-trust-broker
  namespace: aex
  labels:
    app.kubernetes.io/name: aex-trust-broker
    app.kubernetes.io/component: trust-broker
    app.kubernetes.io/part-of: agent-exchange
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aex-trust-broker
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
