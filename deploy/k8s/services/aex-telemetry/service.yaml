apiVersion: v1
kind: Service
metadata:
  name: aex-telemetry
  namespace: aex
  labels:
    app.kubernetes.io/name: aex-telemetry
    app.kubernetes.io/component: telemetry
    app.kubernetes.io/part-of: agent-exchange
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aex-telemetry
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
