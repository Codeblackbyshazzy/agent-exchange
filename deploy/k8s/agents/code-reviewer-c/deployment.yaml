apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-reviewer-c
  namespace: aex
  labels:
    app.kubernetes.io/name: code-reviewer-c
    app.kubernetes.io/component: code-review-agent
    app.kubernetes.io/part-of: agent-exchange
  annotations:
    description: "ArchitectAI - Premium architecture review agent ($25 + $5/file)"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: code-reviewer-c
  template:
    metadata:
      labels:
        app.kubernetes.io/name: code-reviewer-c
        app.kubernetes.io/component: code-review-agent
        app.kubernetes.io/part-of: agent-exchange
    spec:
      containers:
        - name: code-reviewer-c
          image: ${REGISTRY}/code-review-code-reviewer-c:${TAG}
          ports:
            - containerPort: 8102
              protocol: TCP
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: aex-secrets
                  key: ANTHROPIC_API_KEY
            - name: AEX_GATEWAY_URL
              valueFrom:
                configMapKeyRef:
                  name: aex-config
                  key: AEX_GATEWAY_URL
            - name: AEX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: aex-secrets
                  key: AEX_API_KEY
            - name: AGENT_HOSTNAME
              value: "code-reviewer-c"
            - name: CONFIG_PATH
              valueFrom:
                configMapKeyRef:
                  name: aex-config
                  key: CONFIG_PATH
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8102
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8102
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
