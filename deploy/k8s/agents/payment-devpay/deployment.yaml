apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-devpay
  namespace: aex
  labels:
    app.kubernetes.io/name: payment-devpay
    app.kubernetes.io/component: payment-agent
    app.kubernetes.io/part-of: agent-exchange
  annotations:
    description: "DevPay - General dev payments (2% fee, 1% reward on code_review)"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-devpay
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-devpay
        app.kubernetes.io/component: payment-agent
        app.kubernetes.io/part-of: agent-exchange
    spec:
      containers:
        - name: payment-devpay
          image: ${REGISTRY}/code-review-payment-devpay:${TAG}
          ports:
            - containerPort: 8200
              protocol: TCP
          env:
            - name: CONFIG_PATH
              valueFrom:
                configMapKeyRef:
                  name: aex-config
                  key: CONFIG_PATH
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8200
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8200
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
